import 'package:flutter/material.dart';

abstract class AppColors {
  // Color
  static const Color green = Colors.green;
  static const Color blue = Colors.blue;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;

  // Background Color
  static const Color bgAppbar = Colors.green;
  static const Color bgColor = Color(0xFFF4FBF3);
  static const Color bgWhite = Colors.white;
  static const Color bgInfoDelivery = Color(0xFFE8F5E9);
  static const Color activeColor = Colors.green;
  static const Color checkColor = Colors.white;
  static const Color checkedColor = Colors.grey;
  static const Color bgButton = Color(0xFFF37928);
  static const Color bgOutlineGrey = Colors.grey;

  // Text Color
  static const Color textWhile = Colors.white;
  static const Color textBlack = Colors.black;
  static const Color textGreen = Colors.green;

  // Status Color
  static const Color statusGreen = Colors.green;
  static const Color greenWithOpacity = Color.fromRGBO(0, 255, 0, 0.1);
  static const Color statusRed = Colors.red;
  // static const Color redWithOpacity = Color.fromRGBO(255, 0, 0, 0.1);

}